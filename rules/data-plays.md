1. Pick 0 to 2 plays from MENU, only when an authoritative figure would clearly sharpen this story; most stories need none.
2. Never pick a play whose subject the story does not touch.
3. Set seriesId to the bare code exactly as MENU writes it; never a description, never inside query.
4. fred_series: set seriesId. worldbank_indicator and imf_weo: set seriesId and country (ISO code). usaspending_search and wikipedia_summary: set query. eonet_events: set query to the category word. nasdaq_price and edgar_filings: set ticker.

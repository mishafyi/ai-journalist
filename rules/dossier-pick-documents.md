1. Pick up to MAX documents to read in full, most interesting first: those likely to tell a reader something about SUBJECT that the story does not.
2. Prefer documents whose full text can be read over catalogue records.
3. Pick only documents listed in the search results, each once.
4. Copy each ref exactly as the results give it, in its source's format:
   - Federal Register: the document_number.
   - NSArchive: the full path, never the bare id.
   - FRUS: the volume id, a slash, and the document number.
   - EDGAR: the _id exactly, with cik set to the first of its ciks.
   - Wikipedia: the article title.
   - NARA: the naId.
   - Wilson Center: the slug.
   - CIA: the document path from the collection listing.
   - FAS: the page path.
   - Congress: congress number, type and bill number, joined by slashes.
